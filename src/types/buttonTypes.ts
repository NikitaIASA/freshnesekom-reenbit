export enum ButtonVariants {
    PRIMARY = "primary",
    SECONDARY = "secondary",
}

export enum ButtonTypes {
    BUTTON = "button",
    SUBMIT = "submit",
    RESET = "reset",
}

export enum ButtonSizes {
    SMALL = "small",
    MEDIUM = "medium",
    LARGE = "large"
}
